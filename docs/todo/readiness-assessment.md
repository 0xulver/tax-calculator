# Tax Calculator System -- Readiness Assessment

**Date**: April 27, 2026
**Status**: current working assessment

This document supersedes the April 13 snapshot. For concrete filing values,
cross-check with:

- `docs/todo/filing-summary.md`
- `docs/todo/pit38-filing-guide.md`
- `docs/tax-law/16-implementation-audit/filing-policy.md`
- `docs/tax-law/17-pre-residency-usdc-basis/synthesis-pre-residency-usdc-basis.md`
- generated reports in `outputs/`

Important: topic 17 now supersedes topic 16 for PIT-38 mechanics. The current
working model is split-year revenue from `2023-04-12` plus explicit imported
Layer A/B/C basis. The current PIT-38 filing posture is the max no-debt
plus WETH-linked, DOLA-funded, TGE-WFTM OATH and `rf-grain-OP`
imported-basis pool generated in
`outputs/pit38_max_no_debt_plus_oath_weth_dola_tge_rfgrain/`.

April 25 on-chain update: additional EVM chain archives found pre-residency
Ethereum, Polygon, Optimism, Arbitrum, and Fantom activity across known wallets.
Fantom token-transfer logs are now archived through public RPC, and
transaction-level traces are archived for all 2,485 pre-move Fantom tx hashes
found from those logs. OKLink is still needed only for indexed
normal/internal/native Fantom address-history discovery if that gap matters.
Mode, Mantle, Scroll, and the checked Avalanche `frxETH` / `sfrxETH` activity
found so far is post-residency, but still needs normalization for 2024-2025
PIT-38 where it affects crypto costs or fiat disposals.

Move-date inventory update: `private/evidence/onchain/move-date-inventory-2023-04-12/`
now contains a reproducible movement-derived asset snapshot at
`2023-04-12T00:00:00Z`: 6,530 movement rows and 245 non-zero balance rows.
This changes the PIT-38 task from "find the move-date assets" to "maintain
audit support for the selected documented no-debt basis and refine unresolved
rows after filing."

Cost-provenance update: the same folder now contains
`move-date-cost-provenance.csv`, `move-date-cost-provenance-summary.json`, and
`move-date-cost-provenance.md`, generated by
`docs/crypto-transactions/build_move_date_cost_provenance.py`. Current
classification is 28 Layer A direct-asset candidates, 3 unresolved stablecoin
rows (`A/B/C`), 8 direct-or-replacement rows (`A/C`), 80 Layer C DeFi /
replacement-asset rows, 106 Layer D excluded-pending-proof rows, and 20 Layer E
reconciliation/debt/native-flow exceptions. The provisional Koinly 2022
year-end cost-pool cross-check for reviewable rows is about 189,812 SEK. It is
not the full filing value by itself; the selected filing posture adds specific
Layer C candidates from the generated basis-decision workpapers.

Basis-decision update: `move-date-basis-decision.md`,
`move-date-basis-decision-summary.json`, and `move-date-replacement-traces.csv`
quantify the token-transfer-based PIT-38 threshold problem. The reviewable
Koinly 2022 cost-pool cross-check is about `77,955.80 PLN` using the
`2023-04-11` NBP SEK/PLN table rate, against a `116,786.51 PLN` imported-basis
threshold. That old token-transfer-only path is now incomplete because it did
not include protocol-state CDP collateral.

Max-supportable update: `move-date-supportable-basis-candidates.csv` now records
the widest non-duplicative candidate pool. The primary new candidate is the
Ethos `1.77696328 WBTC` trove collateral. The scaled roll-forward now estimates
that path at `142,580.44 PLN`: `41,086.89 PLN` exact Koinly transaction-history
anchors plus `101,493.55 PLN` stablecoin source-open proxy rows. Exact anchors
alone bring the threshold pool to `119,042.69 PLN`, now `2,256.18 PLN`
above the threshold. The full WBTC roll-forward brings it to `220,536.24 PLN`, a
`103,749.73 PLN` surplus if the stablecoin source-open legs and
no-double-counting review are accepted. The selected max supportable no-debt
filing scenario before OATH is `396,449.97 PLN`, adding independent GLP /
`rf-soWETH` / BPT-GTRAIN ETH plus stablecoin-funded GRAIN candidates and still
excluding ERN debt proceeds. The selected urgent filing scenario adds the
WETH-linked component of the WETH/OATH LP (`159,526.15 PLN`), the distinct
DOLA-funded OATH component (`17,172.86 PLN`), the OATH TGE/LGE WFTM
payment-side proof (`15,596.93 PLN`), and `rf-grain-OP` (`102.25 PLN`), and
uses total imported basis of `588,848.16 PLN`. The DOLA source check in
`move-date-dola-oath-source-check.md` confirms the direct DOLA path is an
Arbitrum `crAMM-FRAX/DOLA` LP removal, not visible ERN/CDP debt, and that the
April 10 DOLA units used for OATH and WBTC are separately allocated; the
follow-up `move-date-frax-dola-lp-source-check.md` traces the LP one level
deeper to pre-move stablecoin and LP unwinds. A further Fantom trace now ties
one large bridge-source branch back to the `2022-08-18` Reaper DAI recovery
row with exact 2022 Koinly transaction-history evidence, through
`sDAI -> DIGI #90 -> DAI -> USDC -> Arbitrum USDC.e`. That improves the
provenance for part of the FRAX/DOLA source pool, but does not prove
bank-grade fiat source for the full LP or automatically turn the DIGI yield
uplift into deductible historical cost. The remaining risk is still Layer
C/source-open replacement-basis treatment: original paid acquisition cost or
Swedish surviving replacement basis must be accepted for the upstream
stablecoin/LP value. The remaining
OATH-native bridge/reward/vesting buckets remain excluded. Those
excluded OATH-native buckets are now better source-traced:
`move-date-oath-vesting-trace.md` links `114,148` OATH to the early 2022
distributor and `145,833.3333333332` OATH to recurring batch
vesting/distributor transfers through April 2023, but valuation and tax-basis
treatment remain unresolved.

Unwind update: the same folder now contains `move-date-unwind-workpaper.md`,
`move-date-unwind-summary.json`, and `move-date-unwind-traces.csv`, generated by
`docs/crypto-transactions/build_move_date_unwind_traces.py`. The six reviewed
Layer C positions reconcile exactly to the `2023-04-12` move-date quantities,
and current `balanceOf` checks are zero for all six wrapper/gauge/receipt
tokens. `BPT-RESERVE` is therefore a real move-date holding, but it stays
quarantined for filing because the source/use problem is cost provenance, not
existence.

CDP protocol-state update: `move-date-cdp-positions.md`,
`move-date-cdp-positions.csv`, `move-date-cdp-transactions.csv`, and
`move-date-cdp-positions-summary.json` now query the Optimism Ethos Reserve
trove state at block `89200417` (`2023-04-11T23:59:55Z`). They show an active
`1.77696328 WBTC` trove with `26,341 ERN` protocol debt at the move date.
WETH trove state is zero/non-existent at the move date; WETH collateral activity
starts on `2023-06-29`, after Polish residency began. This means the main CDP
collateral was missing from the movement-derived inventory and must be handled
as protocol-state evidence. ERN-funded LP/gauge positions must not be counted
on top of the WBTC collateral as separate acquisition cost unless a separate
non-debt source is proved.

WBTC basis-trace update: `move-date-wbtc-cdp-basis-trace.md`,
`move-date-wbtc-cdp-basis-trace.csv`, and
`move-date-wbtc-cdp-basis-trace-summary.json` now reconcile four successful
pre-move WBTC trove top-ups to the exact `1.77696328 WBTC` move-date
collateral, identify four relevant cross-chain bridge hops including the
Arbitrum `anyUSDC` receipt back to a Fantom USDC bridge-out, and trace the
visible predecessor path through BTC/WBTC, USDC, DOLA, and wrapper/receipt-token
steps. This improves the evidence map, but it is not a filing value by itself.

Swedish/Koinly evidence update: `move-date-wbtc-swedish-evidence-checklist.md`,
`move-date-wbtc-swedish-evidence-checklist.csv`, and
`move-date-wbtc-swedish-evidence-checklist-summary.json` compare the WBTC trace
against available Koinly transaction-history exports. They find 9 exact Koinly
2022 transaction-hash matches, 45 Jan-Apr 2023 pre-move on-chain rows with no
2023 Koinly export in the repo, and 1 archive-gap/prehistory bucket. This means
the next WBTC task is a Jan-Apr 2023 replacement-basis calculation from the
on-chain trace, not buying another 2022 report.

WBTC roll-forward update: `move-date-wbtc-basis-rollforward.md`,
`move-date-wbtc-basis-rollforward.csv`, and
`move-date-wbtc-basis-rollforward-summary.json` now scale the trace to the
actual WBTC top-up quantities. Current split: `41,086.89 PLN` exact Koinly
anchors, `101,493.55 PLN` stablecoin source-open proxy, and an unresolved
ETH/anyWETH leg for the `0.38977903 WBTC` top-up. The roll-forward is the
current WBTC filing input under the selected posture; the ETH/anyWETH leg stays
excluded.

WBTC stablecoin source-open update:
`move-date-wbtc-stablecoin-source-open.md`,
`move-date-wbtc-stablecoin-source-open-rollforward.csv`,
`move-date-wbtc-stablecoin-source-open-trace.csv`, and
`move-date-wbtc-stablecoin-source-open-transactions.csv` isolate the remaining
source-open judgment. The `101,493.55 PLN` proxy is only two terminal
transactions: `10,000 USDC` on Fantom worth `44,202.01 PLN` and
`13,344.71795066 DOLA` from Arbitrum `crAMM-FRAX/DOLA` removal worth
`57,291.54 PLN`. The Fantom USDC sender
`0xbeb15caee71001d82f430e4deda80e16ddf438db` is not in the current known-wallet
files. User context from April 25: this is likely an
employer/company/Reaper-related address; the taxpayer recalls selling crypto to
stablecoins, investing those stablecoins into Reaper Farm multistrategy vaults
on Fantom, losing those vault positions in a hack/recovery event, receiving
direct in-kind recovery assets, then later using recovered/compensated value to
fund the BTC/WBTC path. This makes the legal/evidence
question narrower: whether these March-April 2023 stablecoin receipts and
LP-removal replacement values can be accepted without buying the 2023 Koinly
export.

Reaper multi-strategy hack/compensation update:
`move-date-reaper-multistrategy-hack-thread.md` now isolates the taxpayer-
specific Reaper thread behind the Fantom USDC row. It finds 44 incident-window
Reaper receipt rows and 7 known-wallet outflows on 2022-08-02/2022-08-03:
`rfETH`, `rfUSDC`, `rfDAI`, `rfUSDT`, `rfBTC`, and `rfWFTM`. The stable receipt
face loss (`rfUSDC` + `rfDAI` + `rfUSDT`) is `96,955.481433717843` receipt
units, and 4 loss transactions match the attacker address named in Reaper's
post-mortem. The loss-to-recovery match now covers every lost receipt family:
`rfUSDC->USDC`, `rfDAI->DAI`, `rfUSDT->fUSDT`, `rfETH->ETH`, `rfBTC->BTC`, and
`rfWFTM->WFTM`; same-family stable direct recovery is `41,002.05546` tokens,
a `0.422896` ratio against the stable receipt face loss. The later 2023-03-18
WBTC-source receipt is `56,314.96 USDC` from
`0xbeb15c...`. This is materially stronger provenance than a memory-only
explanation. The same workpaper now also finds 6 direct 2022-08-18 recovery
rows to the known wallet: `20,000 USDC`, `20,000 DAI`, `4.52241375895353 ETH`,
`0.04636763 BTC`, `1,002.05546 fUSDT`, and `103.128665223373 WFTM`; all 6
share the same recovery caller `0x60bc5e0440c867eeb4cbce84bb1123fad2b262b1`
and target `0xd152f549545093347a162dce210e7293f1452150`. The remaining task is
less "prove the Reaper loss happened" and more "accept the partial recovery
trace, decide the March 2023 compensation/source-open leg, and settle the
Swedish/Polish tax treatment without double counting."

Reaper-to-WBTC link update:
`move-date-wbtc-reaper-recovery-link.md` now links part of the August 18 Reaper
recovery directly to the WBTC predecessor chain. It finds `9,000 DAI` from the
2022-08-18 Reaper DAI recovery and `0.04636763 BTC` from the 2022-08-18 Reaper
BTC recovery in the WBTC trace, with a combined trace proxy of `45,930.36 PLN`.
It also forward-traces the August 18 USDC and ETH recovery rows into the
Arbitrum DOLA/source-open branch as provenance support, not additive filing
amounts. The August 18 fUSDT and WFTM recovery rows remain unlinked to the WBTC
path. The March 18 2023 source-open/compensation leg remains separate:
`10,000 USDC` / `44,202.01 PLN` is allocated to the scaled WBTC roll-forward,
but that transaction is not itself an August 2022 recovery tx.

Basis-trace clarification: if the same WBTC was continuously held through the
move date, merely holding it did not create a Swedish taxable disposal. If the
economic BTC exposure passed through Swedish-taxable pre-move swaps, wrappers,
bridges, or LP/receipt-token steps, the old basis is used for the Swedish
disposal and the replacement asset receives its own acquisition value. The
Polish task is therefore to import the move-date WBTC acquisition/replacement
basis and prove that the same economic cost is not counted twice.

---

## Current Bottom Line

| Area | Readiness | Current Position |
| --- | --- | --- |
| PIT-38 2023-2025 | Ready for selected posture | Use the split-year max no-debt plus WETH-linked, DOLA-funded, TGE-WFTM OATH and `rf-grain-OP` run in `outputs/pit38_max_no_debt_plus_oath_weth_dola_tge_rfgrain/`: imported basis `588,848.16 PLN`, tax `0 PLN`, carry-forward to 2026 `472,061.65 PLN`. This includes WBTC, GLP, `rf-soWETH`, BPT-GTRAIN ETH plus stablecoin-funded GRAIN, the WETH-linked plus DOLA-funded parts of WETH/OATH, OATH TGE/LGE WFTM payment-side proof, and `rf-grain-OP`; it excludes ERN debt proceeds, BPT-RESERVE, and the remaining OATH-native bridge/reward/vesting buckets even though those OATH buckets are now source-traced to distributor/vesting flows. |
| PIT-36 2025 | Mostly ready | Arithmetic report exists: `14,060 PLN` tax due. Classification remains plausible, not risk-free. |
| PIT-28 2025 | Mostly ready | Calculator and report exist. Health deduction has been fixed to reduce revenue, not tax. |
| 2023/2024 PIT-37 -> PIT-36 corrections | Identified | High priority after the April 30 deadline; expected tax impact currently `0 PLN`. |
| Swedish side | Submitted | Swedish report filed by the taxpayer to state Polish tax residency; keep submission confirmation. |

### Current Cash Position For 2025

| Item | Amount |
| --- | ---: |
| PIT-36 tax due | 14,060 PLN |
| Estimated interest on late PIT-36 advances if no advances were paid | ~1,800 PLN |
| PIT-28 tax due | 31,784 PLN |
| PIT-28 payments already made | 32,288.14 PLN |
| PIT-28 expected overpayment | 504.14 PLN |
| PIT-38 tax due | 0 PLN |
| **Net economic cost after PIT-28 refund** | **~15,356 PLN** |

Operationally, do not net the PIT-28 overpayment against PIT-36 unless e-Urzad
or the tax office explicitly allows offset. Pay PIT-36 and interest separately.

---

## What Is Done

### PIT-38 Crypto Engine

Working:

- annual Polish crypto cost-pool model exists,
- Binance, Kraken, FTX, and manual purchase/salary inputs are normalized,
- PIT-38 event reports and detail JSON are generated,
- NBP business-day-before rates are used,
- generated reports include event-level traceability.
- explicit policy modes now exist:
  - `legacy_full_history` for audit/debug only,
  - `split_year_conservative`,
  - `split_year_supportable`,
  - `split_year_high_risk`.

Caveat:

- the current selected PIT-38 reports are in `outputs/pit38_max_no_debt_plus_oath_weth_dola_tge_rfgrain/`;
- the old generated `outputs/pit38_report_*.md` files may still reflect prior
  policy runs;
- regenerate with the command in `docs/todo/pit38-simplified-filing-position.md`
  if imported-cost inputs change again;
- the calculator does not magically reconstruct Swedish acquisition or
  replacement basis for move-date assets; Layer A/B/C imported cost amounts
  must be supplied or computed by a separate inventory ledger.
- on-chain archives added on April 25 show pre-residency Ethereum, Polygon,
  Optimism, Arbitrum, and Fantom activity. The selected max no-debt plus WETH-linked, DOLA-funded, TGE-WFTM OATH and `rf-grain-OP` PIT-38 run
  uses the current move-date workpapers; future normalization can still refine
  the evidence package.
- Fantom token-transfer logs are archived through
  `docs/crypto-transactions/archive_evm_transfer_logs.py`, and pre-move
  transaction traces for the discovered Fantom token-movement tx hashes are
  archived through `docs/crypto-transactions/archive_evm_transaction_traces.py`;
  OKLink normal/internal history still needs `OKLINK_API_KEY` if required.
- the move-date on-chain inventory is generated by
  `docs/crypto-transactions/build_move_date_inventory.py` and stored under
  `private/evidence/onchain/move-date-inventory-2023-04-12/`.
- the move-date cost-provenance workpaper is generated by
  `docs/crypto-transactions/build_move_date_cost_provenance.py` and stored in
  the same folder.
- the move-date basis-decision workpaper is generated by
  `docs/crypto-transactions/build_move_date_basis_decision.py` and stored in
  the same folder.
- the move-date unwind workpaper is generated by
  `docs/crypto-transactions/build_move_date_unwind_traces.py` and stored in the
  same folder.
- the move-date CDP protocol-state workpaper is generated by
  `docs/crypto-transactions/build_move_date_cdp_positions.py` and stored in the
  same folder.

### PIT-36 2025

Working:

- `src/tax_calc/pit36.py` exists,
- `outputs/pit36_report_2025.md` exists,
- 7 pre-JDG USDC payments are included,
- total gross income is `162,735.75 PLN`,
- 20% cost deduction is `32,547.15 PLN`,
- tax due is `14,060 PLN`.

Caveat:

- Art. 13 pkt 8 + 20% KUP is plausible, not fully settled;
- the actual contract facts should support personal-service treatment rather
  than employment or de facto business activity;
- Conclave PDFs still need to be reconciled to the Polygon payment list.

### PIT-28 2025

Working:

- `src/tax_calc/pit28.py` exists,
- `outputs/pit28_report_2025.md` exists,
- Clearstar XML invoices are parsed,
- total JDG revenue is `267,794.30 PLN`,
- health deduction logic has been fixed: 50% of paid health insurance reduces
  revenue, not tax,
- current tax due is `31,784 PLN`,
- current expected overpayment is `504.14 PLN`.

Caveat:

- ZUS DRA declarations show declared health contributions, not necessarily
  proof of payment;
- PIT-28 health deduction should be supported by payment proof or ZUS account
  statement;
- revenue-date logic still needs a final check against service completion,
  invoice issue date, and payment date under Article 14.

### Tax Law Research

Current research coverage:

- topics 1-17 exist in `docs/tax-law/`,
- topic 16 is superseded for PIT-38 filing mechanics,
- topic 17 gives the current split-year / imported-cost policy.

---

## Current Working Filing Values

### PIT-38 Selected Split-Year Max No-Debt Plus WETH-Linked And DOLA-Funded OATH Chain

Use these as the current PIT-38 filing-source values if the selected posture is
accepted.

| Year | Field | Value |
| --- | --- | ---: |
| 2023 | Poz. 34 -- Revenue | 264,113.82 |
| 2023 | Poz. 35 -- Current-year costs | 500,922.56 |
| 2023 | Poz. 36 -- Prior-year costs | 588,848.16 |
| 2023 | Poz. 37 -- Income | 0.00 |
| 2023 | Poz. 38 -- Carry-forward | 825,656.90 |
| 2024 | Poz. 34 -- Revenue | 785,654.54 |
| 2024 | Poz. 35 -- Current-year costs | 488,065.61 |
| 2024 | Poz. 36 -- Prior-year costs | 825,656.90 |
| 2024 | Poz. 37 -- Income | 0.00 |
| 2024 | Poz. 38 -- Carry-forward | 528,067.97 |
| 2025 | Poz. 36 -- Revenue | 271,316.95 |
| 2025 | Poz. 37 -- Current-year costs | 215,310.63 |
| 2025 | Poz. 38 -- Prior-year costs | 528,067.97 |
| 2025 | Poz. 39 -- Income | 0.00 |
| 2025 | Poz. 40 -- Carry-forward | 472,061.65 |

Cross-check: these values come from `outputs/pit38_max_no_debt_plus_oath_weth_dola_tge_rfgrain/`
after including the stablecoin-funded GRAIN leg in the BPT-GTRAIN candidate,
the WETH-linked plus DOLA-funded components of WETH/OATH, and the Binance
Paymonade fiat-counterparty overrides for the June 2025 USDC purchases and the
small November 2024 ETH sale.

### PIT-36 2025

| Item | Amount |
| --- | ---: |
| Gross income | 162,735.75 PLN |
| 20% cost deduction | 32,547.15 PLN |
| Taxable income | 130,188.60 PLN |
| Tax base after 30,000 PLN free amount | 100,189.00 PLN |
| **Tax due** | **14,060 PLN** |

### PIT-28 2025

| Item | Amount |
| --- | ---: |
| Revenue before health deduction | 267,794.30 PLN |
| Health insurance paid per DRA | 5,847.67 PLN |
| Revenue deduction: 50% health insurance | 2,923.84 PLN |
| Taxable ryczalt revenue | 264,870.46 PLN |
| **Tax due** | **31,784 PLN** |
| Payments already made | 32,288.14 PLN |
| **Expected overpayment** | **504.14 PLN** |

---

## Remaining Work Before April 30, 2026

| Priority | Action | Status |
| --- | --- | --- |
| 1 | Submit PIT-38 using the selected split-year max no-debt plus WETH-linked, DOLA-funded, TGE-WFTM OATH and `rf-grain-OP` values if you accept the current evidence posture. | Ready for selected posture |
| 2 | Keep evidence packet with WBTC roll-forward, GTRAIN GRAIN provenance, OATH provenance, supportable candidates, and Swedish/Koinly files for audit support. | Ready enough for filing; can refine after deadline |
| 3 | Check e-Urzad `Zlozone dokumenty` for 2023/2024 PIT-38 status. | Open |
| 4 | File 2023 PIT-38 korekta, 2024 PIT-38 korekta/late filing, and 2025 PIT-38. | Ready after e-Urzad status check |
| 5 | File 2025 PIT-36 and pay `14,060 PLN` plus exact late-advance interest. | Ready arithmetically |
| 6 | File 2025 PIT-28 and claim / leave the `504.14 PLN` overpayment. | Ready arithmetically |
| 7 | Save UPO confirmations for every submission. | Open |

---

## Remaining Work After April 30

| Priority | Action | Why |
| --- | --- | --- |
| High | Correct 2023 PIT-37 -> PIT-36 + optional PIT/ZG. | Wrong form was likely used; PIT/ZG remains fact-dependent. |
| High | Correct 2024 PIT-37 -> PIT-36 + optional PIT/ZG. | Same issue as 2023. |
| High | Consult ZUS specialist on 2023-2025 pre-JDG service income. | Potentially material exposure not solved by PIT forms. |
| High | Refine topic 17 imported-basis reconstruction after filing and decide whether any later PIT-38 amendment is beneficial. | Could increase audit support or add optional basis, but current selected PIT-38 posture is ready. |
| Medium | Prepare KIS individual interpretation request for pre-residency USDC salary basis if the larger pool matters. | Protects aggressive optional bucket. |
| Medium | Resolve 2024-11-04 ETH 0.015 unresolved price event. | Small but should be cleaned up. |
| Medium | Build invoice/payment reconciliation for Conclave USDC. | Supports PIT-36 income and PIT-38 basis. |
| Medium | Obtain ZUS payment proof / account statement for health contributions. | DRA alone is not payment proof. |
| Medium | Reconcile Swedish K4 / Swedish tax filings against imported pre-residency costs. | Needed for Polish PIT-38 evidence if importing pre-residency costs. |
| Medium | Normalize archived Optimism, Arbitrum, Mantle, Mode, and Scroll on-chain data into PIT-38 events. | Koinly exports are stale and omit material self-custody activity. |
| Done | Swedish tax report / residency notice. | Submitted by the taxpayer; keep confirmation and any Skatteverket response. |

---

## Evidence Gaps

### PIT-38

- Topic 17 legal policy is adopted, the imported-basis provenance ledger exists,
  and the selected max no-debt plus WETH-linked, DOLA-funded, TGE-WFTM OATH and `rf-grain-OP` filing amounts are generated.
- Raw Coinbase export is still missing; manual purchase rows need primary support.
- Imported pre-residency costs need Swedish K4/Koinly acquisition or
  replacement-basis proof, plus a no-double-counting review.
- The old safe-default chain is obsolete because it used a synthetic
  pre-residency pool.
- Generated PIT-38 reports should only be used from the selected
  `outputs/pit38_max_no_debt_plus_oath_weth_dola_tge_rfgrain/` run.
- Ethereum raw archive shows pre-residency activity from `2020-12-05` to
  `2023-03-31` across old EVM wallets.
- Polygon raw archive shows pre-residency activity from `2021-09-19` to
  `2023-04-02`.
- Optimism raw archive shows pre-residency activity from `2022-07-09` to
  `2023-04-10`, including ERN/CDP-style positions around the move date.
- Arbitrum raw archive shows pre-residency activity from `2023-03-16` to
  `2023-04-11`.
- Fantom token-transfer logs are archived from public RPC from `2021-09-03` to
  `2024-09-27` across known wallets. Per-transaction traces are archived for
  2,485 pre-move Fantom token-movement tx hashes, yielding 79,562 trace entries
  from `2021-09-03` to `2023-04-07` with zero RPC failures. Native-only FTM
  transfers and transactions with no token `Transfer` log still need OKLink or
  another indexed source if material.
- The move-date inventory exists, with 6,530 parsed movement rows, 245 non-zero
  balance rows, 20 reconciliation exceptions, and 59 likely spam/airdrop rows.
  It is stored in `private/evidence/onchain/move-date-inventory-2023-04-12/`.
- The generated provenance workpaper classifies the 245 rows into review layers:
  28 `A`, 3 `A/B/C`, 8 `A/C`, 80 `C`, 106 `D`, and 20 `E`. Rows in `D` and
  `E` should not be imported unless evidence changes.
- The basis-decision workpaper shows the direct/Koinly-matched reviewable pool
  is not enough by itself: about `77,955.80 PLN` vs a `116,786.51 PLN`
  threshold. The updated scenarios show `119,042.69 PLN` with only the Ethos
  WBTC exact Koinly anchor subset, which now clears the threshold slightly,
  `220,536.24 PLN` when the full WBTC
  scaled roll-forward is accepted, `396,449.97 PLN` for the max supportable
  no-debt path before OATH, and `588,848.16 PLN` for the selected urgent path
  that adds the WETH-linked WETH/OATH component plus the DOLA-funded OATH
  component. The selected path remains
  a Layer C/high-risk posture, but it is the current filing input.
- The unwind workpaper verifies that the six reviewed Layer C positions existed
  at the move date and now have zero latest wrapper/gauge balances. It also
  records the post-move exits: GLP unwound to `USDC.e 10539.680018`,
  `rf-soWETH` to `WETH 0.603672288028468693`, BPT-GTRAIN to WETH/ERN/GRAIN,
  and BPT-RESERVE to USDC/ERN. This supports existence and tracing, but it does
  not by itself make any debt-sourced cost, or the same economic basis counted
  elsewhere, importable.
- The CDP workpaper shows the move-date Ethos trove was WBTC-backed, not
  WETH-backed: `1.77696328 WBTC` collateral and `26,341 ERN` protocol debt at
  block `89200417`. WETH trove activity begins only on `2023-06-29`.
- The WBTC CDP basis-trace workpaper reconciles four successful pre-move WBTC
  top-ups to the exact move-date collateral, identifies four relevant
  cross-chain bridge hops including the Arbitrum `anyUSDC` receipt back to
  Fantom USDC, and expands the predecessor map to BTC/WBTC, USDC, DOLA, and
  wrapper/receipt-token steps. It supports the WBTC candidate used in the
  selected filing posture.
- The WBTC Swedish evidence checklist finds 9 exact Koinly 2022 transaction
  matches and 45 Jan-Apr 2023 pre-move rows without a 2023 Koinly export.
  Further self-calculation can improve audit support after filing.
- The WBTC roll-forward scales the trace to top-up quantities: `41,086.89 PLN`
  exact Koinly anchors plus `101,493.55 PLN` stablecoin source-open proxy, with
  the ETH/anyWETH top-up leg still unresolved and excluded from that amount.
- The stablecoin source-open proxy is now isolated to two terminal transactions:
  `10,000 USDC` on Fantom (`44,202.01 PLN`) and `13,344.71795066 DOLA` from
  Arbitrum `crAMM-FRAX/DOLA` removal (`57,291.54 PLN`). The deeper stablecoin
  trace rows are fallback provenance context, not additive filing values. The
  DOLA-funded OATH check shows the same April 10 LP removal also funded a
  separate `4,000 DOLA` OATH swap and a separate `4,000 DOLA` WETH swap; those
  allocations consume distinct units from the `21,344.719438193822835596 DOLA`
  received, so the selected OATH and WBTC branches do not immediately reuse the
  same DOLA units. The Fantom USDC sender
  `0xbeb15caee71001d82f430e4deda80e16ddf438db` is not in
  the current known-wallet files and is now believed to be an
  employer/company/Reaper-related address tied to later recovered value from the
  Reaper vault hack/loss path. The direct Reaper hack/recovery event is now
  supported on-chain, and part of the August 18 recovery is now tied directly
  to the WBTC path. The FRAX/DOLA follow-up also ties one large bridge-source
  subchain through the August 18 DAI recovery, then through `sDAI`,
  `DIGI #90`, Fantom USDC, and the Arbitrum USDC.e bridge. Remaining work is
  final Swedish/Polish treatment and acceptance of the March-April 2023
  source-open stablecoin legs.
- The Reaper multi-strategy workpaper strengthens that narrative with on-chain
  rows: the known wallet lost `rfETH`, `rfUSDC`, `rfDAI`, `rfUSDT`, `rfBTC`,
  and `rfWFTM` receipt tokens on August 2/3, with `96,955.481433717843` stable
  receipt units in `rfUSDC` + `rfDAI` + `rfUSDT`. The loss-to-recovery match
  covers all six families: `rfUSDC->USDC`, `rfDAI->DAI`, `rfUSDT->fUSDT`,
  `rfETH->ETH`, `rfBTC->BTC`, and `rfWFTM->WFTM`; same-family stable direct
  recovery is `41,002.05546` tokens. It also finds direct August 18 recovery
  rows of `20,000 USDC`, `20,000 DAI`, `4.52241375895353 ETH`,
  `0.04636763 BTC`, `1,002.05546 fUSDT`, and `103.128665223373 WFTM`. The later
  `56,314.96 USDC` March 2023 receipt is WBTC-source provenance, but it is no
  longer the main evidence that Reaper repayment occurred.
- The Reaper-to-WBTC link workpaper directly links `9,000 DAI` and
  `0.04636763 BTC` from August 18 recovery rows into the WBTC predecessor
  trace, with `45,930.36 PLN` of trace proxy support. It also forward-traces
  August 18 USDC and ETH into the Arbitrum DOLA/source-open branch as
  provenance support, and the FRAX/DOLA source follow-up now traces the
  `sDAI -> DIGI #90 -> DAI -> USDC -> Arbitrum USDC.e` branch back to the
  August 18 DAI recovery. These deeper rows are provenance support, not
  additional additive costs. fUSDT and WFTM remain unlinked to WBTC. The March
  2023 `10,000 USDC` / `44,202.01 PLN` WBTC allocation remains a separate
  source-open compensation leg.
- The imported-basis ledger cannot be based only on CEX/Koinly 2022 holdings,
  and the move-date asset snapshot cannot become a PIT-38 cost number until
  those assets are tied back to unrecovered documented acquisition costs.
- Mode/Mantle/Scroll raw archives show post-residency activity and must be
  normalized before final 2024-2025 PIT-38 values.

### PIT-36

- Conclave PDFs / contracts need reconciliation against the 7 Polygon payments.
- Art. 13 pkt 8 classification should be checked against the actual contract and
  work pattern.
- PIT/ZG remains unsettled; foreign payer alone is not necessarily enough.

### PIT-28

- ZUS DRA is not the same as proof of paid health insurance.
- Revenue recognition uses invoice issue dates; this should be checked against
  service completion and payment dates.
- Monthly payments should be reconciled to actual bank/tax-office payment proof.

### Swedish Side

- Swedish report / residency notice has been submitted by the taxpayer.
- Keep the submission confirmation and any Skatteverket response.
- Swedish K4 / tax return records are still important for Polish PIT-38 if
  importing pre-residency crypto costs, because they help prove which costs were
  already used before the move to Poland.

---

## Obsolete Claims From The April 13 Snapshot

The following older claims are no longer accurate:

- PIT-28 calculator "not started" -- false; it exists.
- PIT-36 calculator "not started" -- false; it exists.
- 2024 PIT-38 costs of `778,181.67 PLN` and 2025 carry of `318,607.72 PLN` as
  filing values -- stale/aggressive; use the selected split-year max no-debt
  plus WETH-linked, DOLA-funded, TGE-WFTM OATH and `rf-grain-OP` values in `outputs/pit38_max_no_debt_plus_oath_weth_dola_tge_rfgrain/`.
- Expected large PIT-28 refund -- stale; after the health-deduction fix the
  expected overpayment is about `504.14 PLN`.
- Research coverage of 14 topics -- stale; topic 17 now exists.
- Swedish side "nothing exists" -- stale; research exists and the taxpayer has
  submitted the Swedish report / residency notice.
