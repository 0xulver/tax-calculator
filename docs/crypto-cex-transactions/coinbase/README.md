# Coinbase Raw Exports

Place Coinbase transaction-history CSV exports in this directory.

When raw Coinbase CSV data is present, `tax_calc` will prefer it over the manual fallback in `docs/crypto-transactions/coinbase-purchases.txt`.
